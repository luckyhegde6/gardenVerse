name: Development Build (Manual)

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Platform to build'
        type: choice
        options: [android]
        default: android

jobs:
  build:
    name: Dev Build
    type: build
    params:
      platform: ${{ inputs.platform || 'android' }}
      profile: development
